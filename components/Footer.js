export default function Footer({modal}) {
    return (
        <footer>
            <button onClick={modal} className="footer__button">
                RULES
            </button>
        </footer>
    )
}
